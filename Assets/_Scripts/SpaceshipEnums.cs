public enum SpaceshipCurrency
{
    Energy,
    PureIridium
}

public enum SpaceshipUpgradeType
{
    Additive,      // Aggiunge velocità (+10 km/s per livello)
    Multiplier     // Moltiplica velocità (x1.1 per livello)
}